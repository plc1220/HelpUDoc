# LightRAG server environment (loaded automatically from .env in the current folder).

# --- Server ---
HOST=0.0.0.0
PORT=9621
WORKING_DIR=./rag_storage
INPUT_DIR=./inputs

# --- LLM (Gemini Developer API) ---
LLM_BINDING=gemini
LLM_MODEL=gemini-3-flash-preview
LLM_BINDING_API_KEY=YOUR_GEMINI_API_KEY

# --- Embeddings ---
# NOTE: `lightrag-server` does not currently expose a `gemini` embedding binding.
# To use Gemini embeddings, route embeddings through the OpenAI-compatible Gemini endpoint.
EMBEDDING_BINDING=openai
EMBEDDING_BINDING_HOST=https://generativelanguage.googleapis.com/v1beta/openai
EMBEDDING_BINDING_API_KEY=YOUR_GEMINI_API_KEY
EMBEDDING_MODEL=gemini-embedding-001
EMBEDDING_DIM=3072

