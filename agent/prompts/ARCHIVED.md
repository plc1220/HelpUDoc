# Archived Prompts

This folder is archived.

The runtime now loads skills from `/skills/<skill-id>/SKILL.md` and uses the general assistant to route requests via skills. These prompt files are retained only for reference/backwards compatibility.

Do not add or edit prompts here unless you are intentionally maintaining legacy behavior.
