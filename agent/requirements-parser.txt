# Prefer CPU-only PyTorch wheels if Docling's optional ML dependencies require
# torch, without making MinerU/RAGAnything part of the runtime.
--index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://pypi.org/simple

# Document parsing stack used by attachment understanding. LightRAG lives in
# requirements-api.txt because it is the retrieval substrate, not the parser.
numpy==1.26.4
docling==2.88.0
pypdf
pymupdf
