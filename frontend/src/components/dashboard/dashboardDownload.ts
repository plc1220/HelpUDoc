// PR 10 (frontend-workspace-split): canonical location is now
// `frontend/src/features/dashboard/components/dashboardDownload.ts`.
// This shim preserves the legacy import path during the rename.
export { downloadDashboardHtmlExport } from '../../features/dashboard/components/dashboardDownload';
