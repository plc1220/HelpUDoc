// PR 10 (frontend-workspace-split): canonical location is now
// `frontend/src/features/settings/components/ToolsTab.tsx`.
// This shim preserves the legacy import path during the rename.
export { default } from '../../features/settings/components/ToolsTab';
export type { AgentConfig } from '../../features/settings/components/ToolsTab';
